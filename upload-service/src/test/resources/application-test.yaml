application:
  object-storage:
    host: host
    region: region
    bucket-name: bucket-name
    credentials:
      secret-key: secret_key
      access-key: access_key



spring:
  flyway:
    enabled: false

